import 'package:flutter/material.dart';

class MangeUserScreen extends StatelessWidget {
   static const routeName = "/mangeUser";
  const MangeUserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}