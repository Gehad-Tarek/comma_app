import 'package:flutter/material.dart';

class SupscriptionScreen extends StatelessWidget {
  const SupscriptionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}